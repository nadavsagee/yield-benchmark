# Phases 1-2 (generation + scoring): pure numpy/pandas/scipy
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3      # unsupervised pre-step (isolation forest, PCA) in Phase 3

# Phase 3 (agent): Anthropic API for the LLM tool-use loop
anthropic>=0.40

# optional: nicer tables / plots for the leaderboard later
tabulate>=0.9
matplotlib>=3.8
